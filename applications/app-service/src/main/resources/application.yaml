##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: ms-authentication
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:

adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "db_autenticacion_crediya"
    schema: "public"
    username: "postgres"
    password: "postgres"